# [Eslint](https://eslint.org/)
ESLint statically analyzes JS code to find violations of JavaScript standards or potential errors
in the code.
The rules to be applied are defined in the `config.yml` file and the path to the folder to be analyzed
in the script declaration in `package.json`.
