# [ComposerRequireChecker](https://github.com/maglnet/ComposerRequireChecker)

This tool verifies that the code does not use symbols (classes, functions, etc.) from packages that are not explicitly declared in the `composer.json` file.
