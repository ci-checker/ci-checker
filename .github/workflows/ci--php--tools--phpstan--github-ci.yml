# GENERATED FROM .ci/php/tools/phpstan/github-ci.yml — DO NOT EDIT
name: PHP PHPStan

on:
  pull_request:
    paths:
      - 'src/**'
      - '.ci/php/**'

jobs:
  phpstan:
    uses: ./.github/workflows/ci--php--github-ci.yml
    with:
      script: 'phpstan:ci'
      before_script: 'composer install --no-interaction --ignore-platform-reqs --no-scripts'
